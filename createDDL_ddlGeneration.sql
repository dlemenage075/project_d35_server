CREATE TABLE JOB (ID INTEGER NOT NULL, NAME INTEGER, SALAIRE INTEGER, PRIMARY KEY (ID))
CREATE TABLE PROFILE (ID INTEGER NOT NULL, AGE INTEGER, NAME VARCHAR(255), SURNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PROFILE_JOB (Profile_ID INTEGER NOT NULL, jobs_ID INTEGER NOT NULL, PRIMARY KEY (Profile_ID, jobs_ID))
ALTER TABLE PROFILE_JOB ADD CONSTRAINT FK_PROFILE_JOB_Profile_ID FOREIGN KEY (Profile_ID) REFERENCES PROFILE (ID)
ALTER TABLE PROFILE_JOB ADD CONSTRAINT FK_PROFILE_JOB_jobs_ID FOREIGN KEY (jobs_ID) REFERENCES JOB (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
